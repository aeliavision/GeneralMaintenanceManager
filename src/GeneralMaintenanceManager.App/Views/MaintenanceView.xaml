<UserControl x:Class="GeneralMaintenanceManager.App.Views.MaintenanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="22,18,22,14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Style="{StaticResource EyebrowStyle}" Text="{DynamicResource MaintenanceOperations}"/>
                <TextBlock Margin="0,2,18,0" Text="{DynamicResource Maintenance}" FontSize="24" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>
            <WrapPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Border Margin="0,0,10,0" Padding="8,6" Background="{StaticResource SurfaceTintBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Margin="0,0,8,0" VerticalAlignment="Center" FontWeight="SemiBold" Text="{DynamicResource MaintenanceNumberShortLabel}"/>
                        <TextBox Width="155" Height="32" VerticalContentAlignment="Center" Style="{StaticResource FieldTextBoxStyle}" Text="{Binding MaintenanceReprintNumber, UpdateSourceTrigger=PropertyChanged}"/>
                        <Button Margin="8,0,0,0" MinHeight="32" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding ReprintMaintenanceByNumberCommand}" Content="{DynamicResource OpenMaintenanceByNumber}"/>
                    </StackPanel>
                </Border>
                <Button VerticalAlignment="Center" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding AddMaintenanceCommand}" Content="{DynamicResource AddMaintenance}"/>
            </WrapPanel>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="14,8">
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="6"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="2.2*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="1*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="1*"/></Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0"><TextBlock Style="{StaticResource FieldLabelStyle}" Text="{DynamicResource Search}"/><TextBox Height="32" Style="{StaticResource FieldTextBoxStyle}" Text="{Binding MaintenanceSearchText, UpdateSourceTrigger=PropertyChanged}"/></StackPanel>
                    <StackPanel Grid.Column="2"><TextBlock Style="{StaticResource FieldLabelStyle}" Text="{DynamicResource FromDate}"/><DatePicker Height="32" Style="{StaticResource FieldDatePickerStyle}" SelectedDate="{Binding MaintenanceFromDate, Mode=TwoWay}"/></StackPanel>
                    <StackPanel Grid.Column="4"><TextBlock Style="{StaticResource FieldLabelStyle}" Text="{DynamicResource ToDate}"/><DatePicker Height="32" Style="{StaticResource FieldDatePickerStyle}" SelectedDate="{Binding MaintenanceToDate, Mode=TwoWay}"/></StackPanel>
                </Grid>
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="10"/><ColumnDefinition Width="*"/><ColumnDefinition Width="10"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0"><TextBlock Style="{StaticResource FieldLabelStyle}" Text="{DynamicResource Location}"/><ComboBox Height="32" IsEditable="True" IsTextSearchEnabled="False" Style="{StaticResource EditableSuggestionComboBoxStyle}" ItemsSource="{Binding MaintenanceLocationSuggestions}" Text="{Binding MaintenanceLocationFilter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/></StackPanel>
                    <StackPanel Grid.Column="2"><TextBlock Style="{StaticResource FieldLabelStyle}" Text="{DynamicResource MaintenanceType}"/><ComboBox Height="32" IsEditable="True" IsTextSearchEnabled="False" Style="{StaticResource EditableSuggestionComboBoxStyle}" ItemsSource="{Binding MaintenanceTypeSuggestions}" Text="{Binding MaintenanceTypeFilter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/></StackPanel>
                    <StackPanel Grid.Column="4"><TextBlock Style="{StaticResource FieldLabelStyle}" Text="{DynamicResource PerformedBy}"/><ComboBox Height="32" IsEditable="True" IsTextSearchEnabled="False" Style="{StaticResource EditableSuggestionComboBoxStyle}" ItemsSource="{Binding MaintenancePerformedBySuggestions}" Text="{Binding MaintenancePerformedByFilter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/></StackPanel>
                </Grid>
            </Grid>
        </Border>

        <Border Grid.Row="4" Style="{StaticResource CardStyle}" Padding="0">
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="54"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                <Border Padding="16,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                    <Grid>
                        <StackPanel VerticalAlignment="Center"><TextBlock Text="{DynamicResource MaintenanceLog}" FontSize="14" FontWeight="SemiBold"/><TextBlock Margin="0,2,0,0" Text="{DynamicResource MaintenanceLogHint}" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}"/></StackPanel>
                        <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Orientation="Horizontal">
                            <Button Margin="0,0,8,0" MinHeight="30" Padding="10,4" Style="{StaticResource TertiaryButtonStyle}" Command="{Binding LoadMoreGlobalHistoryCommand}" Content="{DynamicResource LoadMore}" Visibility="{Binding HasMoreGlobalHistory, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <Border Padding="8,3" Background="{StaticResource SecondarySoftBrush}" CornerRadius="10"><TextBlock Text="{Binding MaintenanceLogCount, StringFormat={}{0:N0}}" FontSize="10" FontWeight="SemiBold" Foreground="{StaticResource SecondaryBrush}"/></Border>
                        </StackPanel>
                    </Grid>
                </Border>
                <Grid Grid.Row="1">
                    <DataGrid x:Name="MaintenanceGrid" MouseDoubleClick="MaintenanceGrid_MouseDoubleClick" Style="{StaticResource ModernDataGridStyle}" ItemsSource="{Binding MaintenanceLogItems}" SelectedItem="{Binding SelectedMaintenanceLogItem, Mode=TwoWay}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single" SelectionUnit="FullRow" HeadersVisibility="Column" GridLinesVisibility="None" RowHeight="50" ColumnHeaderHeight="36" BorderThickness="0" EnableRowVirtualization="True" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="{DynamicResource Date}" Binding="{Binding DateText}" Width="0.85*" ElementStyle="{StaticResource DataGridSingleLineTextStyle}"/>
                            <DataGridTextColumn Header="{DynamicResource Location}" Binding="{Binding LocationDisplay}" Width="1.05*" ElementStyle="{StaticResource DataGridSingleLineTextStyle}"/>
                            <DataGridTextColumn Header="{DynamicResource Subject}" Binding="{Binding Title}" Width="1.05*" ElementStyle="{StaticResource DataGridSingleLineTextStyle}"/>
                            <DataGridTemplateColumn Header="{DynamicResource MaintenanceType}" Width="1.15*"><DataGridTemplateColumn.CellTemplate><DataTemplate><Border Style="{StaticResource ActivityBadgeBorderStyle}" HorizontalAlignment="Left" VerticalAlignment="Center"><TextBlock Text="{Binding TypeText, Converter={StaticResource SingleLineTextConverter}, ConverterParameter=22}" Style="{StaticResource ActivityBadgeTextStyle}"/></Border></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="{DynamicResource WorkPerformedDone}" Width="2.5*"><DataGridTemplateColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding Description, Converter={StaticResource SingleLineTextConverter}, ConverterParameter=120}" Style="{StaticResource DataGridSecondarySingleLineTextStyle}" ToolTip="{Binding Description}"/></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn>
                            <DataGridTextColumn Header="{DynamicResource PerformedBy}" Binding="{Binding PerformedBy}" Width="1*" ElementStyle="{StaticResource DataGridSingleLineTextStyle}"/>
                            <DataGridTextColumn Header="{DynamicResource Cost}" Binding="{Binding CostText}" Width="0.7*" ElementStyle="{StaticResource DataGridSingleLineTextStyle}"/>
                            <DataGridTextColumn Header="{DynamicResource HistoryReference}" Binding="{Binding ReferenceNumber}" Width="1.25*" ElementStyle="{StaticResource DataGridSingleLineTextStyle}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Border Background="{StaticResource SurfaceBrush}" IsHitTestVisible="False"><Border.Style><Style TargetType="Border"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding HasItems, ElementName=MaintenanceGrid}" Value="False"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></Border.Style><StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="380"><TextBlock Text="{DynamicResource NoMaintenanceRecords}" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center"/><TextBlock Margin="0,4,0,0" Text="{DynamicResource NoMaintenanceRecordsHint}" TextWrapping="Wrap" TextAlignment="Center" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/></StackPanel></Border>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
