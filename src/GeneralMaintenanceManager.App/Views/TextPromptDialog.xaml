<Window x:Class="GeneralMaintenanceManager.App.Views.TextPromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" Width="520" Height="290" MinWidth="440" MinHeight="250"
        WindowStartupLocation="CenterOwner" Background="{StaticResource AppBackgroundBrush}" ShowInTaskbar="False">
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="74"/><RowDefinition Height="*"/><RowDefinition Height="68"/></Grid.RowDefinitions>
        <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <TextBlock Margin="24,0" VerticalAlignment="Center" Text="{Binding Title}" FontSize="20" FontWeight="SemiBold"/>
        </Border>
        <StackPanel Grid.Row="1" Margin="24,18">
            <TextBlock Style="{StaticResource FieldLabelStyle}" Text="{Binding Label}"/>
            <TextBox Height="86" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Margin="0,8,0,0" Foreground="{StaticResource ErrorBrush}" Text="{Binding ValidationMessage}" TextWrapping="Wrap"/>
        </StackPanel>
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="24,0">
                <Button Width="100" Style="{StaticResource SecondaryButtonStyle}" Content="{DynamicResource Cancel}" IsCancel="True"/>
                <Button Margin="8,0,0,0" Width="110" Style="{StaticResource PrimaryButtonStyle}" Content="{DynamicResource Continue}" Click="Save_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
